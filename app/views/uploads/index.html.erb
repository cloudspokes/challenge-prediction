<div class="well well-small">
	<%= simple_form_for(@upload, url: uploads_challenge_participants_path, :html => {:class => 'form-horizontal' }) do |f| %>
		<%= f.input :data, :as => :file, label: 'challenge-participants-export.csv' %>
	<div class="control-group">
    <div class="controls">
      <button type="submit" class="btn btn-danger">Create Upload</button> <%= @challenge_participants.count %> records
    </div>
  </div>
	<% end %>
</div>

<div class="well well-small">
	<%= simple_form_for(@upload, url: uploads_challlenges_path, :html => {:class => 'form-horizontal' }) do |f| %>
		<%= f.input :data, :as => :file, label: 'challenge-export.csv' %>
		<div class="control-group">
	    <div class="controls">
	      <button type="submit" class="btn btn-warning">Create Upload</button> <%= @challenges.count %> records
	    </div>
	  </div>
	<% end %>
</div>

<div class="well well-small">
	<%= simple_form_for(@upload, url: uploads_challenge_categories_path, :html => {:class => 'form-horizontal' }) do |f| %>
		<%= f.input :data, :as => :file, label: 'challenge-categories-export.csv' %>
		<div class="control-group">
	    <div class="controls">
	      <button type="submit" class="btn btn-success">Create Upload</button> <%= @challenge_categories.count %> records
	    </div>
	  </div>
	<% end %>
</div>

<%= link_to "Back", root_path, :class => 'btn btn-primary' %>